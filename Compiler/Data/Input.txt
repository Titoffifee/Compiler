void f(array<int>a, c.int b){
    int sum = 0;
    for (int i;  0; i=len(a); i+=1)
        { g += a[i] * b;
    }
    return((3));
}

int main() {
    array<int>a[5];
    for(int i = 0; 1; i += 1){
        input(a[i]);
    }
    print(f(a, 4)[5]);
	float x = a[0] | a[1] ^ a[2] + f(a, 4) +;
	print((len(a) * 3) + 12 + x * 5 - (a[0] + a[1] + a[2] + a[3]) % a[4]);
    return (0);
}