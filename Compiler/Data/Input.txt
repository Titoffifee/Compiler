int f(array<int>a.int b){
    int sum = 0;
    for (int i;  0; i=len(a); i+=1 )
        { g += a[i] * b;
    }
    return (sum);
}

int main() {
    array<int>a[5];
    for(int i = 0; i<5; i += 1){
        input(a[i]);
    }
    print(f(a)[5]);
    return (0);
}